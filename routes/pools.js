const express = require('express');
const router = express.Router();

// TODO: Implement pool routes
// GET /pools
// GET /pools/:id
// POST /pools (admin only)
// PUT /pools/:id (admin only)
// DELETE /pools/:id (admin only)

module.exports = router;
